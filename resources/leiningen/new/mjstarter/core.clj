(ns {{namespace}}
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Project {{name}} says: Hello, World!" )
  (println "We're in the following namespace: {{namespace}}")
  )
